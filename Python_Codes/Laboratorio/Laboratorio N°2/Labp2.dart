import dart:io;
void main(){
  //Creamos La primera lista
  List<int>  lista1 = [14,2,5,3,9];
  //Creamos la lista que ingresa el usuario (Lo cual no funciona y le damos valores)
  List<int> lista2 = [1,2,3,4,5];
  //Para la lista random se realizaba este import random = random(); y se usaba una funcion en la cual seria(-16)-25 para obtener una lista random 
  //P
  //Cuando tengamos el resultado de esta lista para agregar los elementos 17 y 24 utilizamos la funcion insert() en la posicion designada
  //Para ordenar de manera desendiente la lista usamos el .sort(reverse=True) y la imprimimos y mostrara la lista resultante de manera decreciente
}